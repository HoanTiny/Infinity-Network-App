import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#246AA3',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default createTheme(theme);
